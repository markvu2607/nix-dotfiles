# Home Manager Configuration

Quick start:

- `sh <(curl --proto '=https' --tlsv1.2 -L https://nixos.org/nix/install)`
- `git clone https://github.com/markvu2607/nix-dotfiles.git ~/.config/home-manager`
- `cd ~/.config/home-manager`
- `nix run .#homeConfigurations.markvu2607.activationPackage`

